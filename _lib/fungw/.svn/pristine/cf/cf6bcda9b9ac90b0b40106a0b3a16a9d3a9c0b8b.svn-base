# Public function; atoi() is provided by another objects through fungw
def hello(how_many, who):
	print "Hello " + str(how_many) + " " + str(who)
	print "atoi: " + str(atoi("42"))
	return 66

print "hello world from init " + str(atoi("33"))

# register hello(), to be called from other objects and the host application
fgw_func_reg("hello")

